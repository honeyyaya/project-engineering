# C++ Implementation

- 默认使用 RAII、值语义和最小可变状态；裸 owning pointer 不作为接口所有权表达。
- 在接口处明确 `const`、引用/值传递、移动语义、异常或错误码约定和线程安全性。
- 资源获取与释放成对出现；异常安全至少满足基本保证，关键更新提供强保证或事务语义。
- 公开头文件保持轻量，避免把实现细节、平台宏和不稳定依赖扩散到调用方。
- 变更后覆盖边界条件、失败路径、并发/生命周期场景，而不只测试 happy path。
