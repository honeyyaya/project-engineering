# Error Handling

先区分可恢复业务失败、输入错误、基础设施失败和编程错误，再选择返回值、结果类型、异常、日志或用户提示。

- 错误必须保留可诊断上下文，同时避免泄露敏感数据。
- 不吞掉错误，不用空 catch、魔法默认值或仅打印日志代替契约。
- 异步 API 定义错误到达方式、取消与超时；UI 层将技术错误映射为可行动反馈。
- 记录重试、幂等和部分成功语义，避免重复副作用。
