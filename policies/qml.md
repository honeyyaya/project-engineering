# QML

- QML 负责声明式展示和轻量交互，业务规则与持久化放在 C++/领域层。
- 暴露给 QML 的 property、model、invokable 和 signal 形成公开契约；定义默认值、变更通知、生命周期和错误反馈。
- 避免深层级双向绑定、隐式单例状态和在 delegate 中执行昂贵或有副作用的操作。
- 检查组件销毁/重建、异步回调回收、模型重置和导航返回时的状态一致性。
