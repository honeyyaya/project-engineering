# Layering

推荐按职责分层：表示层（QML/Widgets）→ 应用编排层 → 领域/用例层 → 基础设施适配层。层间只能通过稳定契约通信。

- 表示层不直接操作持久化、网络或线程资源。
- 领域层不依赖 Qt UI 类型；若必须使用 Qt 基础类型，限定在明确的适配边界。
- 基础设施细节不得向上泄露连接、文件句柄或第三方 SDK 对象。
- 每次跨层调用都定义同步/异步、取消、超时和错误传播语义。
