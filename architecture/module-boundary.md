# Module Boundary

模块以稳定职责和可测试契约划分，而不是按文件数量或类数量划分。

- 每个模块有单一的业务/技术理由，公开面最小化。
- 依赖指向抽象和稳定方向；禁止为了复用而跨层访问内部实现、UI 状态或数据库细节。
- 通过接口、值对象、消息或 Qt model/view 契约传递数据；避免共享可变全局状态。
- 明确谁创建、持有、销毁和修改对象，以及跨线程传递方式。
- 模块边界变更必须记录调用方、测试、构建目标和迁移顺序。

检查时优先搜索 include/import、链接目标、注册表、信号槽连接和反射字符串，因为这些常形成隐式依赖。
