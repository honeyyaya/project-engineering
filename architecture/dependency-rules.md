# Dependency Rules

- 依赖图应保持有向无环；出现环时优先提取稳定抽象或拆分职责。
- 高层策略不依赖低层细节；细节通过接口、适配器或依赖注入接入。
- 公共头文件不得泄露不必要的第三方和平台类型；优先前置声明和 pImpl（确有 ABI/编译隔离收益时）。
- 构建依赖、运行时依赖和测试专用依赖分别记录；不要用测试目标依赖掩盖生产缺失。
- 新增依赖必须说明版本、许可证、安全更新策略和替代方案。
