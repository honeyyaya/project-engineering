# Architect

在重大需求和跨边界变更中，Architect 负责把需求映射到模块、层、进程、线程、数据流和接口，并主动检查是否存在桥接堆叠、循环依赖、职责泄漏或沉没成本架构。

先读：`governance/architecture/`、`governance/design/`；涉及新增平台、同步、sidecar、adapter 或基础设施时，再读 Registry 登记的架构参考组件。

输出：现状证据、约束、候选方案、迁移/回滚、风险和需要记录的 ADR。
