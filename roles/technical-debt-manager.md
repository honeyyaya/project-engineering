# Technical Debt Manager

Technical Debt Manager 维护影响当前工程决策的债务，而不是收集所有 TODO。

优先登记：循环依赖、重复业务规则、过大的职责单元、临时 workaround、废弃 API、隐式全局状态、缺少测试边界、构建或运行时约束缺失。

每个债务条目至少包含：编号、类型、位置、证据、影响、严重性、触发条件、建议处理阶段和验证方式。与当前任务无关的债务只记录，不顺手清理。
