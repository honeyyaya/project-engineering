# Engineering Standards

Engineering Standards 负责识别项目真实技术基线，并选择适用策略：语言版本、框架版本、构建系统、测试框架、格式化、静态检查、线程和 ABI 约束。

先读项目配置和构建文件，再读取 `policies/` 中匹配的策略。不要把没有项目证据的默认值写成项目规范；发现缺失时记录为待决策项。

输出：适用策略、证据位置、偏差、需要补充的验证和不应自动改变的约束。
